app.controller('AlertController', function($scope, AlertMetricsService, $document) {
    $scope.alert = AlertMetricsService;

    // $scope.combats        = $scope.alert.metrics.combats.data;
    // $scope.combatHistorys = AlertMetricsService.metrics.combatHistorys.data;
    // $scope.mapInitials    = AlertMetricsService.metrics.mapInitials.data;
    // $scope.maps           = AlertMetricsService.metrics.maps.data;
    // $scope.outfits        = AlertMetricsService.metrics.outfits.data;
    // $scope.players        = AlertMetricsService.metrics.players.data;
    // $scope.populations    = AlertMetricsService.metrics.populations.data;
    // $scope.vehicles       = AlertMetricsService.metrics.vehicles.data;
    // $scope.weapons        = AlertMetricsService.metrics.weapons.data;
});
